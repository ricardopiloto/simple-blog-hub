# Variáveis usadas pelo frontend (Vite). O projeto roda sem este arquivo (padrão: BFF em http://localhost:5000).
# Variáveis da API (ex.: Admin__Email) devem ser configuradas no ambiente ao rodar a API (dotnet run) ou em appsettings.Development.json, não aqui.

VITE_BFF_URL=http://localhost:5000
