# Variáveis usadas pelo frontend (Vite). O projeto roda sem este arquivo;
# o frontend usa por padrão http://localhost:5000 para o BFF.
VITE_BFF_URL=http://localhost:5000

# Variáveis da API (Admin__Email, connection string, etc.) não são lidas deste arquivo.
# Configure-as no ambiente ao rodar a API (ex.: Admin__Email=seu@email.com dotnet run)
# ou em backend/api/appsettings.Development.json.
