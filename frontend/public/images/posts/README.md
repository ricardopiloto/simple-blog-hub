# Imagens de capa dos posts (upload local)

As imagens de capa enviadas pelo autor no formulário de post são guardadas neste diretório em desenvolvimento (o BFF grava aqui quando `Uploads:ImagesPath` não está definido). Em produção, o caminho de gravação é configurável e deve ser servido pelo servidor web em `/images/posts/` (ver documentação de deploy).
