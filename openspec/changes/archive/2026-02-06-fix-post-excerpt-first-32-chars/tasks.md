# Tasks: Resumo automático com primeiros 32 caracteres

## 1. Ajuste no formulário de post

- [x] 1.1 Em PostEdit, ao alterar o Conteúdo: definir o Resumo automaticamente como os primeiros 32 caracteres do conteúdo (ex.: `content.slice(0, 32).trim()` ou manter até 32 caracteres). Garantir que o resumo seja atualizado sempre que o conteúdo mudar (para refletir os 32 primeiros caracteres), evitando que fique apenas um caractere.
- [x] 1.2 Validar no navegador: digitar no conteúdo e conferir que o resumo exibe até 32 caracteres.
