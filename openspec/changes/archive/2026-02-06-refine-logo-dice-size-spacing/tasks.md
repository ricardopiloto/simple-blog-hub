# Tasks: refine-logo-dice-size-spacing

## 1. Tamanho do ícone

- [x] 1.1 Em `Header.tsx`, alterar a classe do `DiceIcon` no logo de `h-7 w-7` para `h-8 w-8`.
- [x] 1.2 Em `Footer.tsx`, alterar a classe do `DiceIcon` na área de marca de `h-6 w-6` para `h-7 w-7`.
- [x] 1.3 Em `Footer.tsx`, alterar a classe do `DiceIcon` na linha de copyright de `h-5 w-5` para `h-6 w-6`.

## 2. Espaçamento ícone–texto

- [x] 2.1 Em `Header.tsx`, no `Link` do logo, alterar `gap-2` para `gap-1.5`.
- [x] 2.2 Em `Footer.tsx`, no `Link` da área de marca, alterar `gap-2` para `gap-1.5`.
- [x] 2.3 Em `Footer.tsx`, no `span` que envolve ícone e "noDado RPG" na linha de copyright, alterar `gap-1.5` para `gap-1`.

## 3. Validação

- [x] 3.1 Executar `npm run build` e `npm run test`; confirmar que não há erros.
- [x] 3.2 Verificar visualmente que o dado está um pouco maior e o texto mais próximo do ícone no header e no footer.
