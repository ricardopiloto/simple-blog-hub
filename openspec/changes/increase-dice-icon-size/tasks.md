# Tasks: increase-dice-icon-size

## 1. Ajuste de tamanho do ícone

- [x] 1.1 Em `Header.tsx`, alterar a classe do `DiceIcon` no logo de `h-6 w-6` para `h-7 w-7`.
- [x] 1.2 Em `Footer.tsx`, alterar a classe do `DiceIcon` na área de marca de `h-5 w-5` para `h-6 w-6`.
- [x] 1.3 Em `Footer.tsx`, alterar a classe do `DiceIcon` na linha de copyright de `h-4 w-4` para `h-5 w-5`.

## 2. Validação

- [x] 2.1 Executar `npm run build` e `npm run test`; confirmar que não há erros.
- [x] 2.2 Verificar visualmente que o ícone do dado aparece um pouco maior no header e no footer.
