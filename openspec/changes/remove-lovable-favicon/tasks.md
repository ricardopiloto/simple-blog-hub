# Tasks: remove-lovable-favicon

## 1. Favicon

- [x] 1.1 Em `index.html`, no `<head>`, adicionar `<link rel="icon" href="/dice-icon.svg" type="image/svg+xml">` (após as meta tags existentes ou antes de `</head>`).
- [x] 1.2 Remover o arquivo `public/favicon.ico` (favicon do Lovable).

## 2. Validação

- [x] 2.1 Executar `npm run build`; confirmar que não há erros.
- [x] 2.2 Verificar no navegador que a aba do site exibe o ícone do dado (ou que o coração do Lovable não aparece mais).
